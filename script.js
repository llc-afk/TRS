const n=x=>new Intl.NumberFormat('pt-BR',{maximumFractionDigits:4}).format(x),v=(f,k)=>Number(f.elements[k].value),vals=(f,...ks)=>ks.map(k=>v(f,k)),ok=xs=>xs.every(x=>Number.isFinite(x)&&x>=0);const calc={sin:f=>`sen(${n(v(f,'a'))}°) = ${n(Math.sin(v(f,'a')*Math.PI/180))}`,cos:f=>`cos(${n(v(f,'a'))}°) = ${n(Math.cos(v(f,'a')*Math.PI/180))}`,tan:f=>{let a=v(f,'a'),c=Math.cos(a*Math.PI/180);if(Math.abs(c)<1e-10)throw Error('Tangente indefinida neste ângulo.');return `tan(${n(a)}°) = ${n(Math.tan(a*Math.PI/180))}`},hyp:f=>{let x=vals(f,'a','b');if(!ok(x))throw Error('Informe medidas válidas.');return`Hipotenusa = ${n(Math.hypot(...x))}`},leg:f=>{let[a,b]=vals(f,'a','b');if(!ok([a,b])||b>a)throw Error('A hipotenusa deve ser maior que o cateto.');return`Cateto = ${n(Math.sqrt(a*a-b*b))}`}};const m=(keys,fn,label)=>(f=>{let x=vals(f,...keys);if(!ok(x))throw Error('Informe medidas válidas.');return`${label} = ${n(fn(...x))}`});Object.assign(calc,{rectarea:m(['a','b'],(a,b)=>a*b,'Área'),triarea:m(['a','b'],(a,b)=>a*b/2,'Área'),circlearea:m(['a'],a=>Math.PI*a*a,'Área'),squarep:m(['a'],a=>4*a,'Perímetro'),rectp:m(['a','b'],(a,b)=>2*(a+b),'Perímetro'),circ:m(['a'],a=>2*Math.PI*a,'Circunferência'),cube:m(['a'],a=>a**3,'Volume'),box:m(['a','b','c'],(a,b,c)=>a*b*c,'Volume'),cylinder:m(['a','b'],(a,b)=>Math.PI*a*a*b,'Volume'),cone:m(['a','b'],(a,b)=>Math.PI*a*a*b/3,'Volume'),sphere:m(['a'],a=>4*Math.PI*a**3/3,'Volume')});document.querySelectorAll('.tab').forEach(t=>t.onclick=()=>{document.querySelectorAll('.tab,.panel').forEach(x=>x.classList.remove('active'));t.classList.add('active');document.getElementById(t.dataset.tab).classList.add('active')});document.querySelectorAll('form').forEach(f=>f.onsubmit=e=>{e.preventDefault();let o=f.querySelector('output');try{o.textContent=calc[f.dataset.op](f);o.className='ok'}catch(e){o.textContent=e.message;o.className='err'}});
